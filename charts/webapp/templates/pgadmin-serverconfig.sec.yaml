{{- if .Values.global.postgres.enabled -}}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "svc-postgres.pgadminServerConfigSecret" .Subcharts.postgres | quote }}
type: Opaque
stringData:
  servers.json: |
    {
      "Servers": {
        "1": {
          "Name": "PostgreSQL Database",
          "Group": "Servers",
          "Host": "{{ include "svc-postgres.clusterName" .Subcharts.postgres }}-rw.{{ .Release.Namespace }}.svc.cluster.local",
          "Port": {{ required "global.postgres.database.port is required when global.postgres.enabled=true" .Values.global.postgres.database.port }},
          "MaintenanceDB": {{ required "global.postgres.database.name is required when global.postgres.enabled=true" .Values.global.postgres.database.name | quote }},
          "Username": {{ required "global.postgres.database.owner is required when global.postgres.enabled=true" .Values.global.postgres.database.owner | quote }},
          "SSLMode": "prefer",
          "SavePassword": true
        }
      }
    }
{{- end -}}
