apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "svc-mongodb.fullname" . }}-test-connection"
  labels:
    {{- include "svc-mongodb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "svc-mongodb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
