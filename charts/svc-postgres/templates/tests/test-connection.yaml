apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "svc-postgres.fullname" . }}-test-connection"
  labels:
    {{- include "svc-postgres.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "svc-postgres.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
